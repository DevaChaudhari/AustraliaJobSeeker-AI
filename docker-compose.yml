services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      PORT: "8000"

  a2a:
    build: .
    command: ["python", "-m", "australiajobseeker.a2a_server.main"]
    ports:
      - "9999:9999"
    env_file: .env
